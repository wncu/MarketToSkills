### 你只需写一章
主题：[章节 title]
章节编号 N：[N]
总章节数 total：[total]
本章字数上限：{per_chapter_chars} 字符（来自 profiles.json）

## ⚠️ 语言强制规则
这篇报告的语言是 **{LANG}**。你的**所有输出**（章节正文、说明、任何文本）必须严格使用此语言。
- 指令文件可能使用另一种语言，这**不是**你输出的语言
- 无论收到什么语言的指令，都输出 **{LANG}**
- 输出前自检：我这一句是 {LANG} 吗？

**严禁写内联代码**（`python -c`、PowerShell 等）。字数统计只能用 `dr_tools.py word-count`。

### 标题规则（重要）
- **不要写 `##` 章节标题**——`## 一、章节标题` 由装配阶段自动添加，你只需写正文
- **子节标题使用 `### N.1`、`### N.2`… 格式**，其中 N 为上述章节编号。不可使用汉字编号
- 正文第一行直接是 `>` 核心判断（不要在前面空行留白）
- 标题是判断句（使用与主题一致的语言，英文主题用英文，中文主题用中文）

### 子节结构说明
本章预定义了以下子节：`[sections 列表]`。请严格按此列表划分，不可增减。

### 格式
- 使用与主题一致的语言（英文主题用英文，中文主题用中文），引用来源使用 `(N)` 格式对最终读者显示，但在撰写时使用 `[N]` 占位。例：`该市场2025年估值145亿美元[3]` 或 `The market was valued at $145B in 2025[3]`
- **不要在 [N] 后加链接**——引用转换和参考章节由 `convert_citations` 自动处理
- 表格中的 `[N]` 同样只写编号，不加链接
- 不要在正文中使用 `（机构，年份）` 格式
- 每章以 `>` 引用格式的核心判断开头
- **正文段落 ≥ {min_paragraphs} 段（整章合计）**，数据表 ≥ 3 张
- 建议字数约 {per_chapter_chars} 字符。**直接一次写成，不要检查字数、不要反复调整。** 字数由装配阶段统一校验。
- 矛盾数据并排呈现而非掩盖

### 子节写作规范
每个子节应按以下节奏展开：
1. **结论先行**：本子节的核心判断（第一段直接给出）
2. **数据支撑**：用至少一个量化数据或具体案例支撑判断
3. **因果分析**：解释数据背后的逻辑（为什么是这个结果）
4. **反方/补充**：必要时呈现争议或限定前提
5. **过渡**：最后一句自然连接到下一子节（不是"综上所述"式重述）

### 写作质量要求
- **预测/估算数据措辞**：引用的事实如果来自考前预测或未公布数据（如录取率、分数线预测），正文必须使用 "预估""预计""预测" 等限定词，不得当作既定事实陈述。例：✅"据历史数据估算整体录取率约85%" ❌"整体录取率为85%"
- 禁止在子节末尾写 "In summary" "To summarize" "综上所述" "总而言之" 等独立重述段落。
- 禁止每节结尾都用同一句式模板（如 "这凸显了…的重要性" "This underscores the importance of…" 全篇重复使用）。
- 表格前后必须有分析性文字——表格不解释自己。
- 不要连续 3 段以上用同一句式开头。
- 每个子节至少包含一个量化数据或具体案例。
- **禁止使用公式语法**：不得出现 `$...$`、`$$...$$`、`\[...\]` 等 LaTeX/math 语法。指标用纯文本（如 "增长 23%"、"P/E 倍数为 15"、"A = πr²"）。
- **货币符号规范**：货币金额中的 `$` 必须写为 `\$`，避免被 markdown 渲染器错误解析为 LaTeX 数学公式。✅ `\$87K` ❌ `$87K`。反引号内的代码引用和表格格式化不受此限。

### 作业
直接使用 `write` 工具创建 `{TMPDIR}/chapters/chapter-{N}.md`，写入填充完成的完整正文。不需要运行任何工具，不需要创建 manifest 文件。

在回答中只返回文件路径。

---
```
deep-research by hoolulu · github.com/hoolulu/deep-research
```
